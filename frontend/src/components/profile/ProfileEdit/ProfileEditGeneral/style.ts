import vars from "@/styles/vars";
import styled from "styled-components";

export const Subtitle = styled.div`
  margin-bottom: 20px;

  h4 {
    font-weight: normal;
    font-size: 1.2rem;
  }
`